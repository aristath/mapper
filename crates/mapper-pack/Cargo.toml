[package]
name = "mapper-pack"
version = "0.1.0"
edition = "2021"
description = "Offline city-pack tooling for Mapper"
license = "GPL-2.0-only"
publish = false

[dependencies]
sha2 = "0.10"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tar = "0.4"
